import { Display, ControlPanel } from '../../components';

import { Container } from './styles';

export const Main = () => {
  return (
    <Container>
      <Display />
      <ControlPanel />
    </Container>
  );
};
